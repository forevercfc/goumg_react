import { Fragment } from "react";
import ReactDOM from "react-dom";
import { App } from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
//ReactDOM이 render()함수를 제공한다, 첫번재 인수로 render하는 대상 두밴째 인수로 render위치를 지정
